<h1><%= @artist.name %></h1>
<h3><%= @artist.name %> has <%= pluralize(@artist.song_count, "song")  %>. </h3>
<ul>
  <% @artist.songs.each do |s| %>
    <li><%= link_to s.title, s %></li>
  <% end %>
</ul>